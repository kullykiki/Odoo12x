<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dc.list">
      <field name="name">dc list</field>
      <field name="model">dc.dc</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dc.action_window">
      <field name="name">dc window</field>
      <field name="res_model">dc.dc</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dc.action_server">
      <field name="name">dc server</field>
      <field name="model_id" ref="model_dc_dc"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dc" id="dc.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dc.menu_1" parent="dc.menu_root"/>
    <menuitem name="Menu 2" id="dc.menu_2" parent="dc.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dc.menu_1_list" parent="dc.menu_1"
              action="dc.action_window"/>
    <menuitem name="Server to list" id="dc" parent="dc.menu_2"
              action="dc.action_server"/>
    -->
  </data>
</odoo>