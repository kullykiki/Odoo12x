<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fund.list">
      <field name="name">fund list</field>
      <field name="model">fund.fund</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fund.action_window">
      <field name="name">fund window</field>
      <field name="res_model">fund.fund</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fund.action_server">
      <field name="name">fund server</field>
      <field name="model_id" ref="model_fund_fund"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="fund" id="fund.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fund.menu_1" parent="fund.menu_root"/>
    <menuitem name="Menu 2" id="fund.menu_2" parent="fund.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fund.menu_1_list" parent="fund.menu_1"
              action="fund.action_window"/>
    <menuitem name="Server to list" id="fund" parent="fund.menu_2"
              action="fund.action_server"/>
    -->
  </data>
</odoo>